server:
  port: 8181
spring:
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: ALWAYS
greeting-service:
  greeting: "Ciao"
  default-value: "mundo"